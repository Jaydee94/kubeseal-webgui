
{{- if .Values.ui.route.enabled -}}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "kubeseal-webgui.fullname" . }}-ui
  labels:
    {{- include "kubeseal-webgui.labels" . | nindent 4 }}
{{- with .Values.ui.route.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
spec:
{{- if .Values.ui.route.hostname }}
  host: {{ .Values.ui.route.hostname | quote }}
{{- end }}
  to:
    kind: Service
    name: {{ include "kubeseal-webgui.fullname" . }}-ui
    weight: 100
  port:
    targetPort: ui
  {{- if .Values.ui.route.tls.enabled }}
  tls:
    termination: {{ .Values.ui.route.tls.termination }}
    insecureEdgeTerminationPolicy: {{ .Values.ui.route.tls.insecureEdgeTerminationPolicy }}
  {{- end }}
  wildcardPolicy: None
{{- end }}
